@model IschoolJason.AboutModel
@{
    ViewData["Title"] = Model.pageTitle;
}
<h1 class="title">@ViewData["Title"]</h1>

<section class="about">
    <p>@Model.description</p>
    <p>@Model.quote</p>
    <p> >-- @Model.quoteAuthor --<</p>

</section>